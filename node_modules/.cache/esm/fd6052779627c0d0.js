let loginAdmin;_962‍.x([["default",()=>_962‍.o]]);_962‍.w("../../controllers/user",[["loginAdmin",["loginAdmin"],function(v){loginAdmin=v}]]);

const _962‍anonymous=async (req, res) => {
  try {
    const { email, password } = req.body;
    let user = await loginAdmin({ email, password });
    req.session.user = {
      id: user._id,
      name: user.name,
      email: user.email,
      lastLoggedIn: user.lastLoggedIn,
    };
    return res.redirect("/admin/dashboard");
  } catch {
    res.redirect("/admin/login");
  }
};_962‍.d(_962‍anonymous);
