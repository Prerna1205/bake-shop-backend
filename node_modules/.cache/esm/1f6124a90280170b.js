let Post;_028‍.x([["getAllPosts",()=>getAllPosts],["getPost",()=>getPost],["createPost",()=>createPost],["deletePost",()=>deletePost],["getFlaggedPosts",()=>getFlaggedPosts],["approvePost",()=>approvePost]]);_028‍.w("../models/post",[["default",["Post"],function(v){Post=v}]]);

       const getAllPosts = () => {
  return Post.find({ isApproved: true }).populate("user", "name _id");
};

       const getPost = (id) => {
  return Post.findById(id).populate("user", "name _id");
};

       const createPost = ({ title, content, user }) => {
  return Post.create({ title, content, user });
};

       const deletePost = (id) => {
  return Post.findByIdAndDelete(id);
};

       const getFlaggedPosts = () => {
  return Post.find({ isApproved: false }).populate("user", "name _id");
};

       const approvePost = (id) => {
  return Post.findByIdAndUpdate(id, { isApproved: true });
};
